"use strict";

const initial_gridSize = 16;

drawGrid(initial_gridSize);





function drawGrid(gridSize) {
  const gridContainer = document.getElementByClass("grid-container");
  const gridFragment = new documentFragmant();

  
}